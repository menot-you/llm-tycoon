{
  "name": "llm-tycoon-client",
  "description": "LLM Tycoon thin client — Preact + Canvas ASCII. See /GAME_CONCEPT.md",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
